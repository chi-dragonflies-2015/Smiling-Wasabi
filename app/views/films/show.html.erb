<% if current_user == nil %>
  <%= render 'login' %>
<% else %>
  <%= render 'logout' %>
<% end %>
<div class="general-container">
    <div class="col s12 m7">
      <div class="card photo_card">
        <div class="card-image">
          <img src=" <%= @film.movie_art %> ">
          <span class="card-title"><%=@film.title %></span>
        </div>
      </div>
  </div>
  <div class="col s12 m6">
    <div class="card blue-grey darken-1">
      <div class="card-content white-text">
        <span class="card-title"><%= @film.title %></span>
        <% @film.genres.each do |genre| %>
        <h5>Genre: <%= genre.name %><br>
        <% end %>
        <h5>Description:</h2> <%= @film.description %>
      </div>
      <div class="card-action">
        <h3>Rating: <%= @film.review_score %></h3>
        <%= render partial: "/votes/vote", locals: { voteable: @film, vote_route: "/films/#{@film.id}"}  %>
      </div>
    </div>
  </div>

  <ul class="collection">
    <li class="collection-item avatar">
      <% @reviews.each do |review| %>
        <img src="<%= review.user.avatar %>" alt="" class="circle">
        <span class="title"> <%= review.user.name %> </span>

        <p> <%= review.content %> <br/>

        </p>
        <%= render partial: "/votes/vote", locals: { voteable: review, vote_route: "/reviews/#{review.id}"}  %>
        <br><br>
      <% end %>
    </li>
  </ul>


  <div class="comments">
   <ul class="collection">
    <li class="collection-item avatar">
      <% @comments.each do |comment| %>
        <img src="<%= comment.user.avatar %>" alt="" class="circle">
        <span class="title"> <%= comment.user.name %> </span>

        <p> <%= comment.content %> <br/>

        </p>
        <%= render partial: "/votes/vote", locals: { voteable: film, vote_route: "/comments/#{comment.id}"}  %>
        <br><br>
      <% end %>
    </li>
  </ul>

  </div>


  <%= paginate @reviews %>

</div>
